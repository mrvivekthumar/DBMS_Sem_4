CREATE TABLE CUST(
	CNO VARCHAR(5) PRIMARY KEY CHECK (CNO LIKE 'C%'),
	CNAME VARCHAR(20) CHECK (CNAME = UPPER(CNAME)),
	OCCUPTION VARCHAR(25)
);

SELECT SNAME
FROM SALESMAN
WHERE SNAME LIKE '_a%';

SELECT * FROM CUSTOMERS 
WHERE STATE IS NULL;

SELECT * FROM CUSTOMERS ORDER BY CNAME DESC;
